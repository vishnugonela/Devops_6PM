pipeline {
  agent any
  stages {
    stage('System Name') {
      steps {
        sh 'Name: echo -e "Hostname:\t\t\t"`hostname`'

      }
    }
    stage('System Memory') {
      steps {
        sh 'free -m'
      }
    }
  }
}
