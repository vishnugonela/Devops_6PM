pipeline{
        agent any
        stages{
                stage('image_build'){
                        steps{
                         sh '''
                               echo "Building Docker image"
                               docker image build -t image_pipeline docker/Dockerfile
                            '''
                        }
                }
                stage('image_tagging'){
                        steps{
                         sh '''
                               echo "Tagging the image....."
                               docker image tag image_pipeline:latest ashutoshpati/image_pipeline:latest
                            '''
                        }
                }
}
}
